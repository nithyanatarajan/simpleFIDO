# .env - Environment Configuration for passkey_server

# WebAuthn Relying Party info
RP_ID=localhost
RP_NAME="Passkey POC"
ORIGIN=http://localhost:8000

# JWT configuration
JWT_SECRET=super-secure-token
JWT_EXPIRY=60

# CORS (optional)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
